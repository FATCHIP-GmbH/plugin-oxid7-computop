{% extends "layout/base.html.twig" %}

{% block base_js %}
    {{ parent() }}
    {# if oViewConf.isPaypalActive() #}
    {# endif #}
    {% if oViewConf.isFatchipComputopCreditcardActive() and oViewConf.isPaymentCheckoutStep()  %}
        {# script({ include: oViewConf.getModuleUrl('fatchip_computop_payments','js/fatchipComputopCreditCard.js'), priority: 30, dynamic: __oxid_include_dynamic }) #}
    {% endif %}
{% endblock %}

{% block base_style %}
    {{ parent() }}
{% endblock %}
